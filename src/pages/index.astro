---
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<title>{SITE_TITLE}</title>
		<meta name="description" content={SITE_DESCRIPTION} />
		<script type="application/ld+json">
		{
			"@context": "https://schema.org",
			"@type": "ProfilePage",
			"mainEntity": {
				"@type": "Person",
				"name": "Blitz",
				"alternateName": ["Arthur Blitz", "Wilson"],
				"description": "专注 AI Autonomy Evolving 的工程师，OpenClaw 框架与 SPC-CTX 上下文引擎的作者，CDA（上下文密度对齐）范式的提出者。",
				"url": "https://whtoo.github.io",
				"sameAs": ["https://github.com/whtoo"],
				"knowsAbout": [
					"AI Agent",
					"Context Engine",
					"LLM Context Management",
					"Autonomous Systems",
					"Compiler Construction",
					"Functional Reactive Programming"
				],
				"worksFor": {
					"@type": "Organization",
					"name": "OpenClaw"
				}
			}
		}
		</script>
		<style>
			:root {
				--accent: #2337ff;
				--accent-dark: #000d8a;
				--black: 15, 18, 25;
				--gray: 96, 115, 159;
				--gray-light: 229, 233, 240;
				--gray-dark: 34, 41, 57;
				--gray-gradient: rgba(var(--gray-light), 50%), #fff;
			}
			
			body {
				font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
				margin: 0;
				padding: 0;
				line-height: 1.6;
				color: rgb(var(--gray-dark));
			}
			
			.container {
				max-width: 800px;
				margin: 0 auto;
				padding: 0 1rem;
			}
			
			.header {
				text-align: center;
				padding: 4rem 0 2rem;
			}
			
			.header h1 {
				font-size: 3rem;
				margin: 0 0 0.5rem 0;
				color: rgb(var(--black));
			}
			
			.header p {
				font-size: 1.2rem;
				color: rgb(var(--gray));
			}
			
			.nav {
				display: flex;
				justify-content: center;
				gap: 2rem;
				margin: 2rem 0;
			}
			
			.nav a {
				color: var(--accent);
				text-decoration: none;
				font-weight: 500;
				padding: 0.5rem 1rem;
				border-radius: 0.5rem;
				transition: background 0.2s;
			}
			
			.nav a:hover {
				background: rgba(var(--gray-light), 0.5);
			}
			
			.content {
				padding: 2rem 0;
			}
			
			.highlight-box {
				background: linear-gradient(var(--gray-gradient));
				border-radius: 1rem;
				padding: 1.5rem 2rem;
				margin: 1.5rem 0;
			}
			
			.highlight-box h3 {
				margin-top: 0;
				color: rgb(var(--black));
			}
			
			.cta {
				text-align: center;
				margin: 3rem 0;
				padding: 2rem;
				background: linear-gradient(var(--gray-gradient));
				border-radius: 1rem;
			}
			
			.cta a {
				display: inline-block;
				padding: 0.75rem 1.5rem;
				background: var(--accent);
				color: white;
				text-decoration: none;
				border-radius: 0.5rem;
				font-weight: 500;
				margin-top: 1rem;
			}
			
			.cta a:hover {
				background: var(--accent-dark);
			}
			
			.focus-list {
				list-style: none;
				padding: 0;
			}
			
			.focus-list li {
				padding: 0.75rem 0;
				border-bottom: 1px solid rgba(var(--gray-light), 1);
			}
			
			.focus-list li:last-child {
				border-bottom: none;
			}
			
			.focus-list strong {
				color: var(--accent-dark);
			}
			
			.footer {
				text-align: center;
				padding: 2rem 0;
				color: rgb(var(--gray));
				border-top: 1px solid rgba(var(--gray-light), 0.5);
				margin-top: 3rem;
			}
			
			@media (max-width: 768px) {
				.header h1 {
					font-size: 2rem;
				}
				
				.nav {
					flex-direction: column;
					align-items: center;
					gap: 1rem;
				}
			}
		</style>
	</head>
	<body>
		<div class="container">
			<header class="header">
				<h1>{SITE_TITLE}</h1>
				<p>{SITE_DESCRIPTION}</p>
			</header>
			
			<nav class="nav">
				<a href="/">🏠 首页</a>
				<a href="/blog/">📝 博客</a>
				<a href="/projects/">🚀 项目</a>
				<a href="/about/">👤 关于</a>
			</nav>
			
			<main class="content">
				<section>
					<h2>欢迎来到随性</h2>
					<p>
						这里是 <strong>Blitz</strong> 的技术文档库。当前核心关注领域是 <strong>AI Autonomy Evolving</strong> —— 即如何让 AI Agent 在长程任务中保持方向感、记忆力和自主演化能力。
					</p>
					<p>
						我正在构建 <strong>OpenClaw</strong> AI Agent 运行框架和 <strong>SPC-CTX</strong> 上下文引擎，同时撰写 <strong>CDA（上下文密度对齐）</strong> 书稿，系统梳理理论与实践。
					</p>
				</section>
				
				<div class="highlight-box">
					<h3>核心项目速览</h3>
					<ul class="focus-list">
						<li>
							<strong>OpenClaw</strong> — AI Agent 运行框架，支持多 Agent 协同、ACPX 路由与上下文生命周期管理。
						</li>
						<li>
							<strong>SPC-CTX</strong> — Self-Paced Context Engine，通过 Phase 感知和语义压缩图（SCG）实现上下文「主动对焦」。长任务中 token 稳定在 28-40%。
						</li>
						<li>
							<strong>CDA</strong> — Context Density Alignment，上下文密度对齐范式。核心主张：从「如何放更多信息」到「如何让 LLM 在正确方向上获得足够密度」。
						</li>
					</ul>
				</div>
				
				<section>
					<h2>研究方向</h2>
					<ul>
						<li><strong>AI Agent 自主系统</strong>：长程任务中的方向感保持与记忆治理</li>
						<li><strong>上下文引擎</strong>：动态压缩、语义对齐与 Phase 感知架构</li>
						<li><strong>Agent 运行框架</strong>：多 Agent 协同、工具编排与错误恢复</li>
						<li><strong>结构化推理</strong>：LLM-DFS 思维框架与经验原子化方法</li>
						<li><strong>历史积累</strong>：编译器构造、解释器实现、FRP、汇编与可视化</li>
					</ul>
				</section>
				
				<div class="cta">
					<h3>了解更多</h3>
					<p>查看关于页面，了解 Blitz 是谁、SPC-CTX 是什么、以及 CDA 范式的完整理论框架。</p>
					<a href="/about/">关于 Blitz →</a>
				</div>
			</main>
			
			<footer class="footer">
				<p>© 2026 随性. 保留所有权利.</p>
				<p>术之尽头，源自我心。</p>
			</footer>
		</div>
	</body>
</html>
